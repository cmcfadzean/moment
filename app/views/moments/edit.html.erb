<%= component "nav" do %>
  <%= link_to '🔥 Delete Moment', @moment, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
<%= component "container" do %>
  <%= render 'form', moment: @moment %>
<% end %>